services:
  backend:
    build: .
    container_name: traffic-backend
    restart: unless-stopped
    network_mode: host
    environment:
      DB_HOST: 127.0.0.1
      DB_PORT: "5432"
      DB_USER: kori
      DB_PASSWORD: "123456"
      DB_NAME: traffic
      PORT: "8000"
    volumes:
      - ./videos:/app/videos
